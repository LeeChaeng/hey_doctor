{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "SET NODE_ENV=development && nodemon src/index.js --exec babel-node index.js --presets es2015",
    "build": "babel src -d ../build/backend --presets es2015 && cp ./package.json ../build/backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^2.3.2",
    "density-clustering": "^1.3.0",
    "express": "^4.17.1",
    "path": "^0.12.7",
    "tf-kmeans": "^0.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^2.0.3"
  }
}
